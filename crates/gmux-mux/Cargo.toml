[package]
name = "gmux-mux"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
gmux-pty = { path = "../gmux-pty" }
gmux-vt = { path = "../gmux-vt" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
